define emm = Character('Emma', color="#921212")
label classdirection:
    if class_done == False:
        jump classboring
    elif class_done == True:
        "I already had classes."
        jump gardenuni_start2
   